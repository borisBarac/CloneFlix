// ignore_for_file: public_member_api_docs

import 'package:flutter/material.dart';

abstract class VideoControl extends StatelessWidget {
  const VideoControl({super.key, required this.iconSize});

  final double iconSize;
}
